# Golden run — marketing:agente-publisher-blog

- **Data:** 07/07/2026 · máquina: PC (Windows, Git Bash) · executor literal
- **Cenário:** publicação REAL ponta a ponta do post #101 (`atendimento-whatsapp-fora-horario-comercial`, pilar vendas, satélite) no blog de PRODUÇÃO. Draft real vinha do golden do agente-revisor-blog com veredicto real RETRABALHO MAIOR (2 `[preencher]` dependentes de dado interno) — gate 0.1 abortaria, como manda a skill. Fluxo correto executado: retrabalho do draft SEM inventar fato (frases qualitativas no lugar dos números internos) + re-veredicto roteirizado APROVADO (0 placeholders, fontes nomeadas: HBR 2011 inline, case jurídico referenciando post do corpus). Respostas roteirizadas do diretor documentadas no cenário.
- **Classificação: B-custo, custo mínimo consciente:** 1 hero WebP via gerar-hero-blog (gpt-image-2 HIGH, ~R$1).
- **Resultado: APROVADO.** Post AO AR e validado em prod (`blog.expertintegrado.com.br/blog/atendimento-whatsapp-fora-horario-comercial`: HTTP 200, título e hero renderizados); log de aprovações com a linha do post (commit 5026b77); cross-links bidirecionais nos 3 related VISÍVEIS em prod (commits cdc4a87/2e2c1e5/74bdcdb + f3eafca); nota Brain `1e01z5m5au03`; linha #101 no índice local `expert-brain/outputs/blog-publicados-2026.md` (outputs/ é gitignorado por design — índice é artefato local, sem commit).

## Achados e fixes (marketing 2.13.16)

1. **Hero inexistente passa batido (DEFEITO, reproduzido).** O frontmatter referenciava `/images/<slug>-hero.webp` que NÃO existia no repo — a skill validava o frontmatter mas não o arquivo; publicaria com imagem 404 visível em prod. Fix: validação do Passo 1 ganhou item "o arquivo do heroImage EXISTE no repo" com rota de recovery (rodar `gerar-hero-blog` ANTES + incluir o `.webp` no `git add` do Passo 2).
2. **Auto-deploy do push está MORTO e a skill não sabia (DEFEITO CRÍTICO, reproduzido).** Passo 3 dizia "o push dispara o build automático na Vercel" — falso: o projeto é de time (`team_UAgnWON7MrvFUEjnZinLfUpg`, mesmo bloqueio de 15/06 do Meeting Hub; `gh api .../deployments` vazio após push). Polling deu 404 por 3+ min. Fix: Passo 3 reescrito — push continua obrigatório (fonte da verdade) mas NÃO publica; deploy manual `npx --yes vercel@latest deploy --prod --yes --token "$(op read ...Token_Vercel_Produto_Claude_Eric...)"` (CLI vercel ausente no PC; npx resolve, Ready <60s; SEMPRE `--prod`, NUNCA `vercel build`). Linha do 404 na tabela de recovery atualizada com a causa nº 1 real + diagnóstico do filtro `pubDate <= new Date()`.
3. **Cross-link na 6ª posição do `related` é INVISÍVEL (DEFEITO, reproduzido nos 3 related).** O template (`src/pages/blog/[...slug].astro`) faz `.slice(0, 5)` — os 3 posts relacionados já tinham 5 entradas, o slug novo entrou como 6º e não renderizou em prod (0 ocorrências no HTML pós-deploy). Fix na skill: Passo 4 ganhou a regra do limite de render (array com 5+ → inserir o slug novo na 5ª posição, deslocado vai pro fim, sinalizar 6+ pra curadoria no relatório). Fix no conteúdo: swap 5ª↔6ª posição nos 3 MDX (f3eafca), re-deploy, cross-links validados visíveis (1 ocorrência em cada página).

## Observações (não-defeito)

- Gate 0.1 funcionou como desenhado: veredicto real era RETRABALHO MAIOR e o fluxo parou até existir re-veredicto APROVADO sobre draft corrigido — o retrabalho respeitou a regra "nunca inventar fato" (números internos viraram frases qualitativas).
- Scanner (gate 0.2): VERMELHO 0, amarelo 0 no draft final.
- `grep -qiF` do título "falhou" num primeiro teste por artefato do pipe com `head` mascarando o exit — o re-teste com HTML salvo em arquivo confirmou o título; a linha de recovery "HTTP 200 mas grep não bateu" está correta em não declarar falha.
- Deploy manual observado 2x Ready em <60s (24s e ~40s). Durante o run, OUTRA sessão commitou no repo (`fa4df71`, lote expansão saúde-clínicas já revisado) — o deploy manual publica o working tree inteiro, então trabalho commitado de terceiros sobe junto; comportamento esperado do vercel deploy, registrado como característica operacional.
- Índice de publicados (`expert-brain/outputs/`) é gitignorado — o Passo 6 completa com o Edit local, sem commit (não é defeito; outputs/ é artefato gerado por design).
